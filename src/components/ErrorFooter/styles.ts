import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    paddingVertical: 16,
    paddingHorizontal: 24,
  },
  message: {
    fontSize: 14,
    textAlign: "center",
    marginBottom: 4,
  },
  hint: {
    fontSize: 12,
    textAlign: "center",
  },
});
